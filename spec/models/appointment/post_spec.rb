describe "Appointment API POST Request" do
  it "creates a new appointment"
  it "doesn't create a new appointment if there is no first_name"
  it "doesn't create a new appointment if there is no last_name"
  it "doesn't create a new appointment if there is no start_name"
  it "doesn't create a new appointment if there is no end_name"
end
